{
  "name": "players",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "mfes/players",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project players --web",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/mfes/players",
        "root": "mfes/players",
        "skipNxCache": false
      },
      "configurations": {
        "production": {
          "dev": false
        }
      }
    }
  }
}
